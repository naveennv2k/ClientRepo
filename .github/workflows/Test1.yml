name: run on mac


on: [push,workflow_dispatch]


jobs:
  job1:
    
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: dir 
    - name: run on windows
      run: tar xzf zv_cli11.zip
    - name: ls
      run: dir
   
